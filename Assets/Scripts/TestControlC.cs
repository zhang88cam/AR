using UnityEngine;
using System.Collections;

public class TestControlC : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	
	
	void getCoordinatesFromUI(string message) {
		
		
		Debug.Log("Test data :" + message);
	}
}
